import 'package:flutter/material.dart';

class PlayMixControl extends StatelessWidget {
  const PlayMixControl({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Column(
        children: [],
      ),
    );
  }
}
